[plugins]
stonecutter = "0.9.7"
foojay = "1.0.0"
moddev = "2.0.143"


[dependencies]
junit = "6.1.3"


[neoforge]
loader_version_range = "[4,)"


[tools.oxipng]
version = "10.1.1"
enabled = true


["1.21.1"]
minecraft_version = "1.21.1"
minecraft_version_range = "[1.21,1.21.2)"
neo_version = "21.1.248"
neo_version_range = "[21.0,21.2)"
parchment_mappings_version = "2024.11.17"

jei_version = "19.43.0.392"
jei_version_range = "[19,20)"
jade_version = "15.1.6+neoforge"
jade_version_range = "[15,16)"


["1.21.3"]
minecraft_version = "1.21.3"
minecraft_version_range = "[1.21.3,1.21.4)"
neo_version = "21.3.97"
neo_version_range = "[21.3,21.4)"
parchment_mappings_version = "2024.12.07"

jei_version = "0" # JEI is not compatible
jei_version_range = "[0,)"
jade_version = "16.0.1+neoforge"
jade_version_range = "[16,17)"

#["1.21.4"]
#minecraft_version = "1.21.4"
#minecraft_version_range = "[1.21.4,1.21.5)"
#neo_version = "21.4.157"
#neo_version_range = "[21.4,21.5)"
#parchment_mappings_version = "2025.03.23"
#
#jei_version = "20.0.0.4"
#jei_version_range = "[20.0,20.1)"
#jade_version = "17.3.0+neoforge"
#jade_version_range = "[17,18)"

["1.21.4"]
minecraft_version = "1.21.4"
minecraft_version_range = "[1.21.4,1.21.5)"
neo_version = "21.4.158"
neo_version_range = "[21.4,21.5)"
parchment_mappings_version = "2025.03.23"

# [overrides.<mod_id>."<mc_version>"] overrides the ["<mc_version>"] block above,
# field by field, for that mod_id only. No need to declare every field: whatever's
# missing is inherited from that version's base block. Example:
#
# [overrides.mod."1.21.1"]
# neo_version = "21.1.180"
# neo_version_range = "[21.1,21.2)"
#
# [overrides.mod."1.21.4"]
# parchment_mappings_version = "2025.02.09"

# Version profiles: reusable named lists, so you don't repeat the same list in
# every mod.stonecutter.properties.toml. A mod uses them like this, in its own
# [stonecutter] table, instead of listing "versions" by hand:
#
#   [stonecutter]
#   profile = "legacy"
#   extra_versions = ["26.1"]      # optional: adds versions to the profile
#   exclude_versions = ["1.21.4"]  # optional: removes versions from the profile
#
# If a mod sets "versions" directly, that always wins and
# "profile"/"extra_versions"/"exclude_versions" are ignored --
# 100% backwards-compatible, doesn't break any existing mod.
#
# The per-version data block (minecraft_version, neo_version, etc.) can live
# here (shared) or in the mod's own TOML -- a profile only names versions, it
# doesn't matter where each ["<version>"] block is defined. A mod can also
# declare its own [stonecutter.profiles.*] if it needs a custom profile that
# doesn't make sense to share.
#
# To quickly test a subset without touching any TOML:
#   ./gradlew build -Pstonecutter.versions=1.21.1,26.1
# ---------------------------------------------------------------------------


[stonecutter.profiles.legacy]
versions = ["1.21.1", "1.21.3"]


[stonecutter.profiles.current]
versions = ["1.21.1"]

[mod]
id = "dice_and_delish"
name = "Dice & Delish"
group = "com.panzer.mods"
package = "com.panzer.mods.diceanddelish"
version = "1.0.0"
license = "AGPLv3"
resources_license = "CC-BY-NC-SA-4.0"
authors = "Bichal"
description = "A fresh, lightweight cooking and farming expansion adding wild crops, pole-grown tomatoes, grill workstations, and delicious new recipes to Minecraft."
side = "BOTH"
issues = "https://github.com/PanzerDevOrg/DiceAndDelish/issues"
banner = "banner.png"


[stonecutter]
profile = "legacy"
#extra_versions = ["1.21.5"]
exclude_versions = ["1.21.4"]
vcs_version = "1.21.1"


[integrations]
jade_id = "jade"
jade_type = "optional"
jade_ordering = "AFTER"
jade_side = "CLIENT"

jei_id = "jei"
jei_type = "optional"
jei_ordering = "AFTER"
jei_side = "CLIENT"
